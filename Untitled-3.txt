#include <iostream>

int main(){

    // 1. int-Integer(wole number)
    int age = 18;
    int year = 2024;
    int days = 1.5;

    // 2. Above the integer wasnt included
    /* so we use double to include decimal
    */

    double percentage = 74.8;
    double rate = 84.9;

    // 3. Using single characters

    char grade = 'A';
    char initial = 'D';

    // 4. Boolean (true or false)
    bool student = false;
    bool power = true;
    bool Congress = false;

    //5. Strings(objects representing more than 1 character) 
    std::string name = "Adhiraj";
    std::string MName = "Singh";

    std::cout << "Hello Daddy " << name << MName << '\n';
    std::cout << age << " years old only";
    return 0;
}